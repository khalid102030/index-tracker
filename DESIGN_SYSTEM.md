# نظام تصميم راصد بلس — Dark Terminal Editorial

مرجع بصري ثابت لواجهة راصد بلس. أي شاشة أو مكوّن جديد يجب أن يشتقّ ألوانه وخطوطه ومسافاته من هذا الدليل، حتى تبقى الواجهة عائلة واحدة متجانسة.

الفلسفة الحاكمة: **الشاشة ساكنة وهادئة في وضع الراحة، تنبض فقط حين ينبض السوق.** ‏٩٥٪ من الواجهة حبر هادئ على فحمي مطفي، والـ‏٥٪ المتوهّجة محجوزة للحظات التي تستحق انتباه المستخدم فعلاً. هذا هو سرّ راحة العين في الجلسات الطويلة.

---

## ١· الألوان (Design Tokens)

### طبقات الخلفية
| الاسم | القيمة | الاستخدام |
|---|---|---|
| `--void` | `#050506` | أعمق طبقة (خلف كل شيء) |
| `--bg` | `#0a0a0c` | خلفية الجسم الأساسية |
| `--bg2` | `#0d0e11` | تباين خفيف |
| `--panel` | `#101115` | اللوحات والبطاقات |
| `--panel2` | `#15171c` | العناصر النشطة والمرتفعة |

### الخطوط الشعرية (Hairlines)
| الاسم | القيمة | الاستخدام |
|---|---|---|
| `--hair` | `rgba(255,255,255,.14)` | حدود اللوحات الرئيسية (0.5px) |
| `--line` | `rgba(255,255,255,.10)` | فواصل داخلية |
| `--line2` | `rgba(255,255,255,.055)` | الشبكة الخلفية (أرضية لا محتوى) |

القاعدة: الحدود **تُلمَّح ولا تُصرَّح**. كافية لتفصل المناطق، لا تقطع البصر.

### الحبر — ثلاث درجات لا أكثر
| الاسم | القيمة | الاستخدام |
|---|---|---|
| `--ink` | `#e8eaed` | العناوين والأسعار المهمة |
| `--ink2` | `#c4c8cf` | النصوص التفسيرية |
| `--muted` | `#6d7480` | التسميات والوحدات والشروح |
| `--dim` | `#4a4f58` | العناصر المعطّلة والـ placeholder |

سُلّم واضح من ثلاث درجات يخلق هرمية فورية: العين تعرف أين تنظر أولاً دون جهد. تجنّب إدخال درجات رمادية متقاربة إضافية — تُشوّش ولا تضيف.

### الإشارات المالية — نيون حاد عالي التباين
| الاسم | القيمة | الاستخدام |
|---|---|---|
| `--buy` | `#00ff85` | الشراء / الصعود |
| `--sell` | `#ff2d55` | البيع / الهبوط |
| `--hold` | `#8a929e` | الحياد |

مع توهّج مصاحب (`--buy-glow` / `--sell-glow`) وخلفيات خافتة جداً (`--buy-bg` بشفافية ‏.08‎).

### السماوي الكهرومغناطيسي — Electric Cyan
| الاسم | القيمة | الاستخدام |
|---|---|---|
| `--cyan` | `#00e5ff` | خطوط المؤشرات الفنية + العناصر البنيوية |
| `--cyan-glow` | `rgba(0,229,255,.5)` | التوهّج |
| `--cyan-bg` | `rgba(0,229,255,.07)` | خلفية العنصر المختار |

---

## ٢· القواعد الذهبية لراحة العين

**١· التوهّج عملة نادرة — النيون للحدث لا للحالة الساكنة.**
الأخضر/الأحمر النيون يظهران فقط عند: كلمة القرار النهائي (شراء/بيع)، ووميض تغيّر السعر اللحظي. الأرقام الثابتة على الشاشة تُعرَض بحبر أبيض هادئ (`--ink`) لا بلون نيون. بهذا تبقى الشاشة ساكنة، والعين تلتقط الحركة فوراً حين تحدث لأنها الشيء الوحيد المضيء.

**٢· السماوي = الهيكل لا الزينة.**
يُحصر في وظيفتين: خطوط المؤشرات الفنية (RSI/MACD)، والعناصر البنيوية التفاعلية (التبويب النشط، حد الحقل عند التركيز، أزرار الفعل). لا يُرَش على النصوص العامة. حين يكون السماوي "علامة تفاعل" ثابتة، يتعلّم المستخدم لغته بصرياً ويرتاح.

**٣· الأخضر/الأحمر = قرار مالي. السماوي = بيانات فنية.**
فصل بصري صارم يمنع الالتباس: مخرجات الوتد الهابط و MACD و RSI سماوية دائماً، لأنها بيانات فنية لا توصية شراء/بيع.

**٤· صفر انحناء.**
كل زاوية حادة تماماً (`border-radius:0` عالمياً) — الطابع الطرفي الاحترافي.

---

## ٣· الخطوط (Typography)

| الدور | الخط | لماذا |
|---|---|---|
| النص العربي | IBM Plex Sans Arabic | حادّ يناسب روح الطرفية، لا مستدير دافئ |
| الأرقام والبيانات | JetBrains Mono | مسافات ثابتة (`tabular-nums`) — الأسعار لا "ترقص" عند التحديث اللحظي |

كل رقم — سعر، درجة، نسبة، رمز سهم — يجب أن يكون بخط Mono لضمان ثبات المحاذاة العمودية.

الرؤوس والتسميات التقنية: JetBrains Mono بحروف كبيرة ومسافات حروف واسعة (`letter-spacing` + `uppercase`) لتعطي نكهة الطرفية.

---

## ٤· اللوحات والمسافات

- حشو داخلي ثابت: **20px** للوحات الرئيسية.
- هامش سفلي موحّد: **14px** بين اللوحات.
- كل لوحة تحمل علامة زاوية سماوية دقيقة (`::before`) — توقيع بصري خفيف يوحّد الهوية.
- الجداول: رؤوس Mono بحروف كبيرة، صفوف مفصولة بخطوط شعرية **لا** بتظليل متناوب (يتجنّب مظهر Excel).

الانتظام في المسافة هو ما يجعل ثماني شاشات مختلفة تبدو من عائلة واحدة.

---

## ٥· الأجواء (Atmospheric Layers)

**طبقة التشويش الرقمي** — البطل الخفي لراحة العين.
تشويش SVG ناعم جداً (`opacity: .035`, `mix-blend-mode: overlay`) يكسر صفاء السطح الداكن كسراً غير مرئي تقريباً، فيقلّل توهّج الشاشة على العين كما تفعل الشاشات المطفية (matte). المستخدم لا يراه واعياً، لكنه يشعر بالفرق بعد ساعة تحليل.

**الشبكة الخلفية** — خطوط 52px بشفافية ‏.055‎ تعطي إحساس شاشة التداول الاحترافية، خافتة كفاية لتبقى أرضية.

**توهّج الأرقام الحية** — ظل نصّي سماوي خفيف (`.live`) على السعر الحالي فقط.

---

## ٦· الحركة الهادفة (Motion)

**الوميض اللحظي (Snap Flash)** — القاعدة الأهم:
عند تغيّر السعر، يومض الرقم دفعة واحدة بتوقيت `steps(1,end)` **بلا `ease-in-out`** — أخضر إن صعد، أحمر إن هبط، ثم يعود فوراً. يعكس نبض السوق الفعلي.

لراحة العين: الومضة خلفية شفافة خافتة (‏.28‎ alpha) تتلاشى فوراً، لا وميض أبيض صارخ. تُلاحَظ بطرف العين، لا تُصفَع بها.

**بقية الحركة**: مؤشر التحميل يدور بخطوات (`steps(12)`) لا بدوران ناعم. شريط التقدّم سماوي متوهّج بانتقال خطّي.

**احترام التفضيلات**: مع `prefers-reduced-motion` تختفي كل الحركة تماماً.

---

## ٧· مؤشر حالة السوق

نقطة في الشريط العلوي محسوبة من توقيت السعودية (الأحد–الخميس ‏9:30–15:20):
- **مفتوح**: نقطة خضراء نيون تنبض (`pulse`) + نص "السوق مفتوح".
- **مغلق**: نقطة حمراء ثابتة + نص "السوق مغلق".

تحاول أولاً قراءة `/api/market-clock`، وتسقط على الحساب المحلي كخط دفاع.

---

## ٨· ما لا يتغيّر

- **كل المنطق البرمجي يبقى كما هو**: الوكلاء، السكانر، راصد، التوصيات، نقاط الاتصال بالـAPI. التغيير بصري بحت.
- **تصدير PDF يبقى فاتحاً**: مستند مطبوع لا شاشة تحليل — تحويله لداكن يهدر الحبر ويضرّ القراءة الورقية.

---

## الخلاصة

عند تصميم أي شاشة جديدة، اسأل: هل هذا العنصر **حدث** يستحق التوهّج، أم **حالة ساكنة** تكتفي بالحبر الهادئ؟ إذا كان الأغلب هادئاً والقليل متوهّجاً، فأنت على المسار الصحيح.
