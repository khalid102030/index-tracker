# نظام تصميم راصد بلس

مرجع بصري ثابت لواجهة **راصد بلس** (تبويبان: التحليل + الإعدادات). أي شاشة أو مكوّن جديد يجب أن يشتقّ ألوانه وخطوطه من هذا الدليل حتى تبقى الواجهة متجانسة.

المزاج المعتمد: **داكن هادئ مريح للعين** — رمادي داكن لا أسود قاتم، وألوان مكتومة لا نيون صارخ. مصمّم لجلسات التحليل الطويلة دون إجهاد بصري.

المبدأ الحاكم: الشاشة هادئة وساكنة، والألوان تخدم الوظيفة لا الزينة. التغيير التصميمي بصري بحت — لا يمسّ أي منطق برمجي.

---

## ١· الألوان (متغيّرات CSS في `:root`)

كل ألوان الواجهة تمرّ عبر هذه المتغيّرات، فتغييرها يحوّل الواجهة كلها بتناسق.

### الخلفيات
| المتغيّر | القيمة | الاستخدام |
|---|---|---|
| `--bg` | `#1a1d23` | خلفية الجسم الأساسية (رمادي داكن هادئ) |
| `--bg2` | `#1e2128` | خلفية الحقول والعناصر الغائرة |
| `--p` | `#22262e` | اللوحات (`.pn`) والبطاقات |
| `--p2` | `#282d36` | العناصر النشطة والأزرار |

### الحدود
| المتغيّر | القيمة | الاستخدام |
|---|---|---|
| `--ln` | `rgba(90,169,216,.20)` | حدود مميّزة (شريط، تبويبات) بلمسة زرقاء |
| `--ln2` | `rgba(255,255,255,.07)` | حدود محايدة رفيعة (لوحات، حقول) |

الحدود بسماكة `0.5px` — رفيعة تُلمَّح ولا تُصرَّح.

### اللون المميّز — أزرق هادئ
| المتغيّر | القيمة | الاستخدام |
|---|---|---|
| `--ac` | `#5aa9d8` | العناصر التفاعلية، التبويب النشط، حدود الأزرار، الأرقام المميّزة |
| `--ac2` | `#7cc0e6` | درجة أفتح للتفاصيل الثانوية |

أزرق مكتوم هادئ (بدل السماوي الصارخ). يُستخدم للتفاعل والتمييز، لا يُرَش على كل النصوص.

### الحبر
| المتغيّر | القيمة | الاستخدام |
|---|---|---|
| `--ink` | `#e4e6ea` | النصوص والعناوين |
| `--mt` | `#7a828f` | التسميات والشروح الجانبية والوحدات |

### الإشارات — مكتومة لا نيون
| المتغيّر | القيمة | الاستخدام |
|---|---|---|
| `--g` | `#4ec9a3` | النجاح / الصعود / الحالة المفعّلة |
| `--r` | `#e07385` | الفشل / الهبوط / الخطأ |
| `--o` | `#d9a25a` | التنبيه / الأزرار عالية التكلفة |

درجات مكتومة مريحة — تُميَّز بوضوح دون أن تصرخ في العين.

---

## ٢· الخطوط

| الدور | الخط | لماذا |
|---|---|---|
| النص العربي | IBM Plex Sans Arabic | حادّ يناسب الطابع الطرفي |
| الأرقام والبيانات | JetBrains Mono | مسافات ثابتة (`tabular-nums`) — الأسعار والدرجات تبقى بمحاذاة ثابتة |

كل رقم — سعر، نسبة، درجة، تاريخ — بخط Mono. الرؤوس والتسميات التقنية بخط Mono بحروف كبيرة ومسافات واسعة لنكهة الطرفية.

---

## ٣· قواعد الاستخدام

**صفر انحناء.** كل الزوايا حادة (`border-radius:0`) — الطابع الطرفي المهني.

**الأزرار هادئة لا مساحات متوهّجة.** الزر خلفية داكنة (`--p2`) بحدّ رفيع ملوّن ونص ملوّن — لا تعبئة صلبة بلون صارخ بعرض الشاشة. زر الفعل عالي التكلفة (`.btbig`) يأخذ اللون البرتقالي `--o` تنبيهاً.

**التبويب النشط بخط سفلي لا خلفية صارخة.** `box-shadow: inset 0 -2px 0 var(--ac)` مع خلفية `--p2` خفيفة.

**الجداول بخطوط شعرية لا مظهر Excel.** رؤوس بخط Mono بحروف كبيرة باللون الأزرق، صفوف مفصولة بخطوط رفيعة.

**الفصل بالمساحة والتباين لا بالأقفاص.** اللوحات (`--p`) أفتح قليلاً من الخلفية (`--bg`) فتنفصل بالتباين، مع حشو داخلي وهوامش موحّدة (padding 20px، margin-bottom 14px).

---

## ٤· أسماء الأصناف الفعلية

| الصنف | الدور |
|---|---|
| `.bar` | الشريط العلوي |
| `.br` / `.lg` / `.nm` | الشعار واسمه |
| `.tabs` / `.tb` / `.tb.on` | شريط التبويبات والتبويب النشط |
| `.pn` | اللوحة/البطاقة الرئيسية |
| `.sh` / `.d` / `.tg` | ترويسة القسم (نقطة + عنوان + وسم) |
| `.bt` / `.btbig` | الأزرار (عادي / كبير عالي التكلفة) |
| `.bs` / `.bs.on` | أزرار صغيرة (تبديل/فلتر) |
| `.rc` | بطاقة نتيجة/توصية |
| `.sb` / `.sf` | شريط تقدّم (خلفية + تعبئة) |
| `.toast` | إشعار عابر |
| `.spin` | مؤشر تحميل دوّار |

عند إضافة أي عنصر جديد، أعِد استخدام هذه الأصناف بدل اختراع أنماط جديدة — هذا ما يحافظ على التجانس.

---

## ٥· الحركة

خفيفة وهادفة: مؤشر التحميل يدور بخطوات (`steps(12)`)، أشرطة التقدّم بانتقال خطّي، اللوحات تظهر بتلاشٍ خفيف (`.fade`). مع `prefers-reduced-motion` تختفي كل الحركة احتراماً لمن يتحسّس منها.

---

## ٦· ما لا يتغيّر

- **كل المنطق البرمجي ثابت**: دوال التحليل، الإعدادات، التيليجرام، المزامنة، التوصيات، ونقاط اتصال الـAPI. أي إعادة تصميم تلمس كتلة `<style>` فقط.
- **أسماء الأصناف والمتغيّرات ثابتة**: تغيير القيم في `:root` يحوّل المظهر دون كسر أي وظيفة.

---

## الخلاصة

راصد بلس واجهة **هادئة داكنة مريحة**: رمادي داكن، أزرق مكتوم للتفاعل، أخضر/أحمر مكتومان للإشارات، حدود رفيعة، وأرقام بخط ثابت المسافات. عند أي إضافة، اسأل: هل تنسجم مع هذا الهدوء؟ إن أضافت لوناً صارخاً أو حدّاً ثقيلاً، فهي تشذّ عن الهوية.
